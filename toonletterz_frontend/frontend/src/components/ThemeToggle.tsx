import React from 'react';
import { motion } from 'framer-motion';
import { Sun, Moon } from 'lucide-react';

// Removed theme toggle component since dark mode is removed
const ThemeToggle: React.FC = () => {
  return null;
};

export default ThemeToggle;